# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Results with autolayouts' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Results with autolayouts

  target 'Results with autolayoutsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Results with autolayoutsUITests' do
    inherit! :search_paths
    # Pods for testing
  end
   pod 'IQKeyboardManagerSwift', '6.3.0'
end
